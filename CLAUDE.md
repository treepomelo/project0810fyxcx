# Autonomous Guidelines (自动执行规则)

- **最大化自主性 (Maximum Autonomy)**：在处理任务时，应当连续、自主地完成多个步骤。**绝对不要**在每个小步骤、常规文件修改或代码编写后询问 "Shall I proceed?" 或等待 Yes/No 确认。如果在处理过程中需要执行工具调用或文件修改，请直接连续批量执行，不要每执行一步就向我寻求文字确认（如问‘我可以这样做吗？’）。
- **默认最佳实践 (Default to Best Practices)**：遇到技术实现细节、命名规范或小型架构选择时，请直接采用当前项目的既定风格和业界最佳实践自行做出决断，并继续执行。
- **仅在以下情况中断 (Stop Only For)**：
  1. 遇到高风险、不可逆的破坏性操作（如删除整个数据库、强制覆盖核心生产配置）。
  2. 出现重大架构分歧，且现有信息无法通过常理推断。
  3. 编译、测试或运行过程中遇到无法自动修复的报错。
- **执行汇报 (Execution Reporting)**：不要一步一问。请一次性完成一个完整的功能模块或逻辑闭环，最后给出清晰的修改总结报告。
## 安全与边界红线 (Security Boundaries)
- **严格限定工作目录**：你的所有操作（文件读写、脚本运行、命令行调用）**必须**严格限制在当前项目根目录以内。
- **绝对禁止的操作**：
  1. 严禁执行任何超出当前项目目录的路径访问（如 `../` 或绝对路径根目录操作）。
  2. 严禁执行高危全局命令（如 `rm -rf /`、全局系统设置修改、未授权的网络包删除等）。
  3. 严禁在未经用户明确同意的情况下删除整个代码仓库或 `.git` 目录。